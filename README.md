# ai-factory-front

Portal React (Vite).

## Dev

```bash
cp .env.example .env
npm install
npm run dev
```

## Docker

```bash
docker build -t ai-factory-front --build-arg VITE_API_URL=https://your-back.railway.app .
```
